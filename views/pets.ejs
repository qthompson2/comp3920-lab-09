<!DOCTYPE html>
<html>
	<head>
		<meta charset="utf-8"/>
		<link rel="stylesheet" href="bulma.min.css">
		<script defer src="https://use.fontawesome.com/releases/v5.3.1/js/all.js"> </script>
	</head>
	<body>
        <table>
            <tr>
                <th>Pet Name</th>
            </tr>
            <% for(let i=0; i < allPets.length; i++) { %>
            <tr>
                <td><%= allPets[i].name %></td>
            </tr>
            <% } %>
            <tr>
                <td>
                    <% if (allPets.length == 0) { %>
                        <p>There are no pets.</p>
                    <% } else if (allPets.length == 1) { %>
                        <p>There is 1 pet.</p>
                    <% } else { %>
                        <p>There are <%= allPets.length%> pets.</p>
                    <% } %>
                </td>
            </tr>
        </table>
    </body>
</html>